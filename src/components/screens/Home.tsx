import { Button, View } from 'react-native';
import Page from '../molecules/Page';

const Home: React.FC = () => {
  return (
    <Page>
      <View>
        <Button title="Go to About" onPress={() => {}} />
      </View>
    </Page>
  );
};

export default Home;
