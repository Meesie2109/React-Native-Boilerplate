import { Button, View } from 'react-native';
import Page from '../molecules/Page';

const Home: React.FC = () => {
  return (
    <Page>
      <View>
        <Button title="I do nothig" onPress={() => {}} />
      </View>
    </Page>
  );
};

export default Home;
